# 结构化法律 Intake

- `sft_all.jsonl`：594 条完整 SFT 数据。
- `sft_train.jsonl`：564 条训练数据。
- `sft_dev.jsonl`：30 条开发数据。
- `confirm120.jsonl`：120 条冻结确证题与标签。
- `contract.md`：输出字段和确定性抽取规则。

对应 Base/模型输出和统计位于 `results/answers/structured/` 与 `results/structured_intake.md`。
